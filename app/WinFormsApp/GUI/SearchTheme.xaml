<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:search="clr-namespace:ICSharpCode.AvalonEdit.Search;assembly=ICSharpCode.AvalonEdit">

    <Color x:Key="BgBase">#1E1E1E</Color>
    <Color x:Key="BgSurface">#252526</Color>
    <Color x:Key="BgElevated">#2D2D30</Color>
    <Color x:Key="BorderSubtle">#3C3C3C</Color>
    <Color x:Key="Accent">#007ACC</Color>
    <Color x:Key="TextPrimary">#D4D4D4</Color>
    <Color x:Key="TextMuted">#9DA1A6</Color>

    <SolidColorBrush x:Key="BrushBgBase" Color="{StaticResource BgBase}" />
    <SolidColorBrush x:Key="BrushBgSurface" Color="{StaticResource BgSurface}" />
    <SolidColorBrush x:Key="BrushBgElevated" Color="{StaticResource BgElevated}" />
    <SolidColorBrush x:Key="BrushBorder" Color="{StaticResource BorderSubtle}" />
    <SolidColorBrush x:Key="BrushAccent" Color="{StaticResource Accent}" />
    <SolidColorBrush x:Key="BrushText" Color="{StaticResource TextPrimary}" />
    <SolidColorBrush x:Key="BrushTextMuted" Color="{StaticResource TextMuted}" />

    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource BrushBgBase}" />
        <Setter Property="Foreground" Value="{StaticResource BrushText}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
        <Setter Property="CaretBrush" Value="{StaticResource BrushText}" />
        <Setter Property="Padding" Value="6,4" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="bd" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3">
                        <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource BrushAccent}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BrushBgElevated}" />
        <Setter Property="Foreground" Value="{StaticResource BrushText}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Padding" Value="6,4" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="3"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#333337"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#2A2A2E"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ToggleButton">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}" />
        <Setter Property="Padding" Value="5,3" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Transparent"
                            BorderThickness="1"
                            CornerRadius="3"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#333337"/>
                            <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource BrushAccent}"/>
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource BrushAccent}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type search:SearchPanel}">
        <Setter Property="Background" Value="{StaticResource BrushBgSurface}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type search:SearchPanel}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Margin="0,4,16,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="6"/>    <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="12"/>   <ColumnDefinition Width="Auto"/> </Grid.ColumnDefinitions>

                            <TextBox x:Name="PART_searchTextBox" 
                                     Grid.Column="0" 
                                     Margin="2,2"/>

                            <Button Grid.Column="1" 
                                    Command="search:SearchCommands.FindPrevious" 
                                    ToolTip="Find Previous (Shift+F3)"
                                    Margin="1,2" Width="24" Height="24" Padding="0">
                                <Path Data="M 0,4 L 4,0 L 8,4" 
                                      Stroke="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" 
                                      StrokeThickness="1.5" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Button>

                            <Button Grid.Column="2" 
                                    Command="search:SearchCommands.FindNext" 
                                    ToolTip="Find Next (F3)"
                                    Margin="1,2" Width="24" Height="24" Padding="0">
                                <Path Data="M 0,0 L 4,4 L 8,0" 
                                      Stroke="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" 
                                      StrokeThickness="1.5" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Button>

                            <ToggleButton Grid.Column="4" 
                                          IsChecked="{Binding MatchCase, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          ToolTip="Match Case" 
                                          Margin="1,2" Width="24" Height="24">
                                <TextBlock Text="Aa" FontSize="11" FontFamily="Segoe UI" FontWeight="SemiBold"/>
                            </ToggleButton>

                            <ToggleButton Grid.Column="5" 
                                          IsChecked="{Binding WholeWords, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          ToolTip="Match Whole Word" 
                                          Margin="1,2" Width="24" Height="24">
                                <TextBlock FontSize="11" FontFamily="Segoe UI" Margin="0,-1,0,0"><Underline>ab</Underline></TextBlock>
                            </ToggleButton>

                            <ToggleButton Grid.Column="6" 
                                          IsChecked="{Binding UseRegex, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          ToolTip="Use Regular Expression" 
                                          Margin="1,2" Width="24" Height="24">
                                <TextBlock Text=".*" FontSize="12" FontFamily="Segoe UI" FontWeight="Bold" Margin="0,-2,0,0"/>
                            </ToggleButton>

                            <Button Grid.Column="8" 
                                    Command="search:SearchCommands.CloseSearchPanel" 
                                    ToolTip="Close (Esc)"
                                    Background="Transparent" BorderBrush="Transparent"
                                    Margin="2,2" Width="22" Height="22" Padding="0">
                                <Path Data="M 0,0 L 7,7 M 7,0 L 0,7" 
                                      Stroke="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" 
                                      StrokeThickness="1.5" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Button>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <SolidColorBrush x:Key="{x:Static SystemColors.InfoBrushKey}" Color="#2A1F1F" />
    <SolidColorBrush x:Key="{x:Static SystemColors.InfoTextBrushKey}" Color="#FFB3B3" />

</ResourceDictionary>